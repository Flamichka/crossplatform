# crossplatform app.name
___
## Instalization
To setup project run:

before installing dependencies make sure the virtual enviroment is active
```
> python -m venv .venv 
> .venv\Scripts\activate
> ./setup.bat
```
## Dev Run

```
> python -m app
```

